version: '3'
services:
  db:
    image: postgres:10
    ports:
      - "5435:5435"
    volumes:
      - postgres-data:/var/lib/postgresql/data
#      - C:\docker\volumes\postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=docker
volumes:
  postgres-data:
#    driver:local
#    external:false
#    mountpoint:":C:\docker\volumes\postgres"
